module.exports = {
  presets: [
    '@babel/preset-env', // Transpile modern JavaScript features
    '@babel/preset-react', // Enable JSX syntax support
  ],
  plugins: [
    // Add any additional Babel plugins if needed
  ],
};
